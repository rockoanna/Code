#include <iostream>
#include <string>
#include <sstream>

using namespace std;


int main()
{
    const string sep="$;!A";
    string txt = "1OANNA$OANNA2;$OANNA3;OANNA4!asdf";
    string rec;
    vector<string> splits;
    for(int i=0;i<txt.size();i++){
        if(txt[i] !='$' && txt[i] !=';'){
           rec.push_back(txt[i]);
        }else{
            if(rec.size() > 0){
                cout <<rec<<endl;
                splits.push_back(re);
                rec.clear();
            }
        }
    }

    if(rec.size() > 0){
                cout <<rec;
     }
    return 0;
}
